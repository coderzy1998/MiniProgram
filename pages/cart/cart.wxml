<!-- pages/cart/cart.wxml -->
<block wx:for="{{cartList}}" wx:key="index">
  <cart-list-item goods="{{item}}" index="{{index}}"></cart-list-item>
</block>
<cart-bottom-bar selected="{{isSelectAll}}" price="{{totalPrice}}" counter="{{totalCounter}}" bindtap="onSelectAll" />